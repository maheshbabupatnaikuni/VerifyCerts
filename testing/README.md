# Test files

Use locally generated synthetic certificates to exercise OCR and tamper-detection workflows. Real certificates, personal records, and generated verification artifacts must not be committed.
